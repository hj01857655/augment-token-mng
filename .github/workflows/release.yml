name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
          - platform: 'macos-latest'
            args: '--target x86_64-apple-darwin'
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4



      - name: Rust setup
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Sync node version and setup cache
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Augment Token Manager v__VERSION__'
          releaseBody: |
            ## 🚀 Augment Token Manager v__VERSION__

            ### 性能优化版本
            - HTTP连接优化，减少50%连接时间
            - 异步文件操作，提升UI响应性
            - 智能缓存系统，减少重复请求
            - 前端打包优化，更快加载速度
            - 依赖精简，减少30%编译时间

            ### 下载说明
            - **Windows**: 下载 `.msi` 安装包
            - **macOS**: 下载 `.dmg` 安装包（支持Intel和Apple Silicon）

            ---
            **致谢**: 基于 [zhaochengcube/augment-token-mng](https://github.com/zhaochengcube/augment-token-mng) 优化改进
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
